// Package count provides utilities for counting environment variable keys
// across one or more deployment targets in an envoy configuration.
//
// It supports per-target counts as well as aggregate totals across all targets,
// and includes a human-readable formatter for CLI output.
package count
